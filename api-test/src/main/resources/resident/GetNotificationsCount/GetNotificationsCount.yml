GetNotificationsCount:
   Resident_GetNotificationsCount_vid_all_Valid_Smoke:
      endPoint: /resident/v1/unread/notification-count
      uniqueIdentifier: TC_Resident_GetNotificationCount_01      
      description: Verify that the system successfully returns the notifications count when the API is called with valid input details and a valid VID
      role: residentNewVid
      checkErrorsOnlyInResponse: true
      restMethod: get
      additionalDependencies: Dependent_ESignet_GenerateToken_02
      inputTemplate: resident/GetNotificationsCount/GetNotificationsCount
      outputTemplate: resident/GetNotificationsCount/GetNotificationsCount
      input: '{
}'
      output: '{
        
}'

   Resident_GetNotificationsCount_uin_all_Valid_Smoke:
      endPoint: /resident/v1/unread/notification-count
      uniqueIdentifier: TC_Resident_GetNotificationCount_02      
      description: Verify that the system successfully returns the notifications count when the API is called with valid input details and a valid UIN
      role: residentNew
      checkErrorsOnlyInResponse: true
      restMethod: get
      additionalDependencies: Dependent_ESignet_GenerateToken_01
      inputTemplate: resident/GetNotificationsCount/GetNotificationsCount
      outputTemplate: resident/GetNotificationsCount/GetNotificationsCount
      input: '{
}'
      output: '{
        
}'

   Resident_GetNotificationsCount_uin_InvalidToken_Neg:
      endPoint: /resident/v1/unread/notification-count
      uniqueIdentifier: TC_Resident_GetNotificationCount_03      
      description: Verify that the system returns an error when the API is called with notifications count of VID using an invalid token
      role: resident
      checkErrorsOnlyInResponse: true
      restMethod: get
      inputTemplate: resident/GetNotificationsCount/GetNotificationsCount
      outputTemplate: resident/error
      input: '{
}'
      output: '{
      "errors": [
    {
      "errorCode": "KER-ATH-401"
    }
  ]
        
}'

   Resident_GetNotificationsCount_vid_InvalidToken_Neg:
      endPoint: /resident/v1/unread/notification-count
      uniqueIdentifier: TC_Resident_GetNotificationCount_04      
      description: Verify that the system returns an error when the API is called with notifications count of VID using an invalid token
      role: resident
      checkErrorsOnlyInResponse: true
      restMethod: get
      inputTemplate: resident/GetNotificationsCount/GetNotificationsCount
      outputTemplate: resident/error
      input: '{
}'
      output: '{
      "errors": [
    {
      "errorCode": "KER-ATH-401"
    }
  ]
        
}'