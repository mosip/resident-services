{
  "id": "mosip.resident.download.personalized.card",
  "version": "1.0",
  "requesttime": "{{requesttime}}",
  "request": {
    "html": "{{html}}",
    "attributes": [
      {{#each attributes}}
        "{{attributesVal}}"
        {{#unless @last}},{{/unless}}
      {{/each}}
    ]
  }
}