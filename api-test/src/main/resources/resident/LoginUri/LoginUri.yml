LoginUri:
   Resident_LoginUri_all_Valid_Smoke:
      endPoint: /resident/v1/login/{redirectURI}
      description: Verify that the system successfully returns the login redirect URI when the API is called with valid input details
      role: state
      checkErrorsOnlyInResponse: true
      restMethod: get
      inputTemplate: resident/LoginUri/LoginUri
      outputTemplate: resident/LoginUri/LoginUriResult
      input: '{
		"redirectURI": "$BASE64URI$"
}'
      output: '{
        
}'
